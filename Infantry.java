public abstract class Infantry extends BasicHero{
    public Infantry(int id, String name, String heroType, int healthLevel, int baseAttack, int initiative) {
        super(id, name, heroType, healthLevel, baseAttack, initiative);
    }
}
